# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0

# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: terraform-cdk-action
description: A GitHub Action for terraform-cdk-action
runs:
  using: node16
  main: dist/index.js
author: HashiCorp, Inc.
branding:
  color: purple
  icon: terminal
inputs:
  cdktfVersion:
    description: The version of cdktf CLI to use
    default: 0.12.2
    required: false
  terraformVersion:
    description: The version of terraform to use
    default: 1.3.0
    required: false
  workingDirectory:
    description: The directory to use for the project
    default: ./
    required: false
  stackName:
    description: The stack to run / plan, only required when the mode is plan 'plan-only' or 'plan-and-apply'
    required: false
  mode:
    description: "What action to take: 'synth-only', 'plan-only', 'plan-and-apply'"
    required: true
  terraformCloudToken:
    description: The terraform cloud / terraform enterprise token to use
    required: false
  githubToken:
    description: The github token to use
    required: false
  commentOnPr:
    description: Whether to comment the plan / the status on the PR
    default: "true"
    required: false
